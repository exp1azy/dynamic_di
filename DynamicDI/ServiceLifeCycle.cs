namespace DynamicDI
{
    /// <summary>
    /// Defines the service lifetime options for dependency injection.
    /// </summary>
    public enum ServiceLifeCycle
    {
        Transient,
        Scoped,
        Singleton
    }
}
