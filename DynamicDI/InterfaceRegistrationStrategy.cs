namespace DynamicDI
{
    /// <summary>
    /// Defines the interface registration strategy for dependency injection.
    /// </summary>
    public enum InterfaceRegistrationStrategy
    {
        FirstOnly,
        AllInterfaces
    }
}
